{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug Ploy",
      "program": "${workspaceFolder}/builddir/ploy",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
